# P3-W56 — Data SoT + contractor IA (2026-07-19)

Следом за journey-аудитом и W55 attention.

## Сделано

| ID | Фикс | Зачем |
|----|------|-------|
| Portal | template literal progress bar | Metro SyntaxError |
| D1 | `expense_from_purchase` на `paid`/`delivered` | fact бюджета = server ledger |
| D1 mobile | skip purchased picks если есть Expense(purchase_id) | без двойного счёта |
| D2 | `project_service.accept_stage` → RuntimeError | только AcceptOrchestrator |
| Pay | create_payment → notify customer | честный «отправлен» |
| Pay | confirm notify type ≠ payment_pending | title confirmed |
| IA | HomeAcceptanceBanner role-aware | contractor: ждут заказчика |
| IA | UnifiedAcceptanceList contractor CTAs | Открыть этап / filter=review |
| IA | legacy/push `/control` contractor → repair?tab=control | не QC |

## Проверка

```bash
cd apps/mobile && npx tsx lib/legacyRoutes.test.ts && npx tsx lib/pushLinks.test.ts
cd backend && .venv/bin/python -m pytest tests/test_purchase_expense_w56.py -q
```

## Остаток (не W56)

- H0 ops (HTTPS + live YuKassa + TestFlight)
- Mutual estimate lock
- Portal schedule/estimate
- Одна поверхность приёмки (banner vs list) — частично смягчено
- Purchase create_from_picks auto-approve
