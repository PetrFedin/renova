# P3-W21 — IA cleanup (audit sprint 2)

## Изменения

- **`menuRoutes()`** — фильтры `readOnly`, `phase`, `excludeIds`; скрыты redirect-only пункты; customer vs contractor приёмка без дубля QC/work-acceptance
- **`HomeScreenBody`** — «Ещё» без notifications (уже на главной); beta analytics/reports только при `phase === complete`
- **`legacyRoutes` + control tab** — customer → `/work-acceptance`, contractor → `/quality-control`
- **`pushLinks`** — role-aware `/control`; unknown notification type → `/notifications`
- **Удалён** неиспользуемый `FinanceCenterScreen.tsx` (маршрут уже redirect в `finance-center.tsx`)

## DoD

- `npm run test:priority` PASS
- Guest/readOnly видит урезанное «Ещё» (documents, notifications, QC)

## Следующее (P3-W22)

- Trust: YuKassa staging keys
- `detailLevel` brief для cosmetic preset
- WorkAcceptance offline UI badges
