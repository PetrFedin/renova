# Mobile parity on main

After slice-6, `main` still had ~52 legacy Expo routes deleted on `develop` (catch-alls replaced them).

This PR removes those stubs and syncs remaining scripts/README/.gitignore with develop.
