# P3-W25 — IA dead ends + offline guards + portal styles

## Изменения

- **portal.tsx** — недостающие стили pay/draft; реквизиты в буфер; ApiError для YuKassa/Контур
- **PaymentDetailSheet** — СБП копирует полные реквизиты (M11)
- **documents/payments API** — offline enqueue только при сетевой ошибке (не 4xx)
- **approvals.tsx** — offline UX approve/reject; empty CTA → смета/changes

## DoD

- `npm run test:priority` PASS
