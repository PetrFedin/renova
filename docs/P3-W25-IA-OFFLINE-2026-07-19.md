# P3-W25 — IA sprint 2 + chain deeplinks + offline rooms

## Изменения

### IA / guest menu
- **`routeRegistry.ts`** — guest/readOnly «Ещё» только `documents` + `notifications` (убран `quality-control` без CTA); `control` в `REDIRECT_ONLY_MENU_IDS`
- **`routeRegistry.test.ts`** — guest не видит QC; лимит ≤3 пункта

### Chain / deeplinks (change_order)
- **`pushLinks.ts`** — `changeOrderEstimateRoute()` + `resolveNotificationLink('change_order')` → объект/смета `estimateLayer=changes` (как `approvalLinks`)
- **`NotificationsScreen`**, **`NotificationCenter`** — change_order игнорирует устаревший `link_path=/approvals`
- **`legacyRoutes.test.ts`** — customer control → work-acceptance, contractor → quality-control

### Portal read-only
- **`portal.tsx`** — `portalReadOnly` / `canPayPortal` / `canAcceptPortal` / `canSignPortal`; оплата и подпись скрыты с понятным текстом

### Field / offline
- **`OsRoomsScreen`** — `isOfflineQueued` для запросов комнат, approve/reject, patchStageRooms, archive
- **`approvals.tsx`** — readOnly guard для guest; offline UX approve/reject

## DoD

- `npm run test:priority` PASS
- `npx tsx apps/mobile/lib/pushLinks.test.ts` PASS
- `npx tsx apps/mobile/lib/routeRegistry.test.ts` PASS
- `npx tsx apps/mobile/lib/legacyRoutes.test.ts` PASS

## Заблокировано

- Live **ЮKassa** — нужны staging keys (`YUKASSA_*`)
- **CI workflow** — `.github/workflows/ci.yml` локально, push без workflow scope
- Portal sign/pay prod — Kontur webhook + keys

## P3-W26 (дополнение в том же спринте)

- **pushLinks** — `change_order` push → смета/changes (changeOrderEstimateRoute)
- **NotificationCenter** — tap change_order → estimate changes
- **approvals** — readOnly guard
- **routeRegistry** — control redirect-only; guest «Ещё» без quality-control
- **OsRoomsScreen** — offline UX для room changes
