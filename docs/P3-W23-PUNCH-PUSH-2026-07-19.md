# P3-W23 — punch web fallback + notification routing

## Изменения

- **FloorPlanPanel** — фото punch на web через file picker; fallback галерея если камера недоступна; offline queue UX; pin с фото = ▣
- **pushLinks** — `change_order` → `/approvals` (не budget/payments)
- **issues API** — offline enqueue только при сетевой ошибке, не при 4xx
- **docs/STAGING-YUKASSA.md** — чеклист live payments

## DoD

- `npm run test:priority` PASS
- `npx tsx apps/mobile/lib/pushLinks.test.ts` PASS
