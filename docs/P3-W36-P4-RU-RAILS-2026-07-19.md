# P3-W36 — P4 RU rails (1С / банк / ФНС / digest / warranty)

**Цель:** поднять RU scale с ~30% без enterprise 1С — практичные CSV + trust ФНС + retention lite.

## Сделано

| Task | Изменение |
|------|-----------|
| P4.1a 1C export | `GET …/export/1c-payments.csv` + Document Center «Выгрузка в 1С» |
| P4.1b Bank register | `GET …/export/bank-register.csv` + «Реестр для банка» |
| P4.1c FNS honesty | Auto-verify demo только development/test; staging/prod — live или «без проверки» |
| P4.2c Digest lite | `POST …/digest/weekly` + push + KPI PDF из Document Center |
| P5.1 Warranty lite | `POST …/warranty-claims` → issue `[Гарантия]` + doc type warranty |
| Cleanup | Удалён orphan `NotificationsScreen` (route → inbox) |

## DoD
- CSV открывается в Excel RU (BOM + `;`)
- Staging не помечает чеки verified без ответа ФНС
- Гарантия видна в QC и Документах

## Не в этом PR
- Live YuKassa keys (ops)
- Полный XML 1С / bank import matching
- Ollama AI text digest
- `ci.yml` workflow push (нужен OAuth scope `workflow`)
