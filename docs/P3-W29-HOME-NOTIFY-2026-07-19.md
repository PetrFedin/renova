# P3-W29 — approvals offline complete + home IA

## Изменения

- **design/floor API** — approve design package + waste order: offline только при сетевой ошибке
- **HomeScreenBody** — readOnly/guest: убран дубль «Уведомления» (остаётся в «Ещё» и профиле)
- **NotificationsScreen** — empty state с CTA «На главную»

## DoD

- `npm run test:priority` PASS
