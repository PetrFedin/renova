# P1 Wave 1 — summary (2026-07-16)

**Branch:** `develop`  
**Base:** `46f78c3` (P0 complete)

## P0.5 ACL (finish)

| Файл | Изменение |
|------|-----------|
| `analytics.py` | `require_project` на analytics, budget-breakdown, category-alerts, forecast, scenario |
| `estimate.py` | `require_project` на все endpoints |
| `export.py` | убран повторный `get_project` после `require_project` |

## P1.6 Automation cron

- `automation_reminders_worker.py` — tick: `scan_project_reminders` + `scan_waste_reminders`
- `main.py` lifespan — background loop (default 900s, `AUTOMATION_REMINDERS_*`)
- `notifications.py` — `/waste-reminders/check` делегирует в worker
- `tests/test_automation_reminders.py` — waste + overdue + manual endpoint

## P1.3 Schedule SoT

- `docs/SCHEDULE-SOT-2026-07-16.md`
- Docstrings: `calendar_service`, `schedule_service`
- `WorkScheduleScreen` — label: график ≠ приёмка

## P1.4 Route registry v2

- `routeRegistry.ts`: inbox, scan-receipt, stage, materials-procurement, conflicts (existing)
- Documents: `entryPoints: ['os.menu']` — dedupe с Home «Ещё»
- `routeRegistry.test.mjs` — новые id

## P1.5 Offline parity

- `offlineErrors.ts` — upload/payment block codes
- `documents.ts` — upload → `offline_upload_blocked`
- `payments.ts` — createPayment → `offline_payment_create_blocked`
- `workAcceptances.ts` — offline queue (P0, без изменений)

## P1.7 Procurement hub (scaffold)

- `docs/PROCUREMENT-HUB-2026-07-16.md`
- Deep link `materials-procurement` → `/repair?tab=materials`

## Staging prep

- `docs/STAGING-URL-CHECKLIST-2026-07-16.md` (EXPO_PUBLIC_API_URL в eas.json)

## Cleanup

- Удалён неиспользуемый `backend/app/api/ws.py` (канон: `api/v1/ws.py`)

## Verification

```bash
cd backend && .venv/bin/python -m pytest tests/test_automation_reminders.py tests/test_acceptance_canon.py tests/test_cross_domain_notify.py -q
npm run test:priority
bash scripts/e2e-smoke.sh   # при :8100 up
```

**Results:** 10/10 backend priority tests PASS · `test:priority` PASS · e2e-smoke PASS

## Осталось (следующие волны)

| ID | Scope |
|----|-------|
| **P1.2 Kontur** | HTTP minimal, webhook → signed_at, mobile polling, sandbox e2e |
| **P1.1 ЮKassa** | Webhook idempotency, `renova://payment-return`, staging test keys, убрать auto-demo Pro |
| P1.7 UI | OsMaterialsHub subtabs Picks·Purchases·Receipts |
| P1.4 legacy | Удаление 5 legacy tabs per sprint |
